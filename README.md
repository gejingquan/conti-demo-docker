How to use this repo to create a new docker container?

```
mkdir Dockerfile
cd Dockerfile
git clone https://github.com/gejingquan/conti-demo-docker
cd conti-demo-docker
docker build -t dockername .
```

Then, the new docker is created.
